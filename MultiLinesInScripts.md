# Multi lines in scripts #

In ACT-Scripts you can define multi lines to avoid long lines.

```
MyCoolCommand abbas test \
              foo \
              bar

MyCoolOtherCommand

MyAnnotatedCommand param1=a \
                   param2=a
```